# (consumer) route group — reserved

Area for the consumer journey (browse offers, reserve/buy — UC08/UC09/UC11).
It stays empty until the corresponding backend APIs exist (backend Sprint 3).
The consumer register/login endpoints (`POST /consumers`, `POST /consumers/login`)
already exist, but no screen is built against them yet.
